storage:
    path: data

rpc:
    network: regtest
    url: http://localhost:18443
    username: foo
    password: rpcpassword
    wallet: test_wallet

settings:
    retention_depth: 100 # Blocks kept on disk.
    catch_up: true
